[
  {
    "_id": "{{@ system @}}-{{@ datatype @}}-all",
    "namespaced_identifiers": false,
    "pump": {
      "run_at_startup_if_not_populated": true
    },
    "source": {
      "operation": "{{@ datatype @}}-list",
      "system": "{{@ system @}}",
      "trace": {
        "log_response_body_maxsize": 1000000
      },
      "type": "rest"
    },
    "type": "pipe"
  },
  {
    "_id": "{{@ system @}}-{{@ datatype @}}-collect",
    "namespaced_identifiers": false,
    "pump": {
      "run_at_startup_if_not_populated": true
    },
    "source": {
      "dataset": "{{@ system @}}-{{@ datatype @}}-all",
      "type": "dataset"
    },
    "transform": {
      "rules": {
        "default": [
          [
            "copy",
            "_id"
          ],
          [
            "merge",
            "_S.{{@ datatype @}}"
          ]
        ]
      },
      "type": "dtl"
    },
    "type": "pipe"
  }
]